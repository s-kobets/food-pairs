[build]
  command = "npm run build"
  publish = "dist"
  functions = "/netlify/functions"

[dev]
  command = "vite"
  targetPort = 3000
  publish = "dist"
  framework = "vite"
  autoLaunch = true

[functions]
  included_files = ["types.js", "types.ts"]